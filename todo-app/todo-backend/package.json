{
  "name": "todo-express-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "REDIS_URL=redis://default:password@127.0.0.1:6379 MONGO_URL=mongodb://the_username:the_password@localhost:3456/the_database nodemon ./bin/www",
    "build&run": "docker build -t express-server . && docker run -p 3000:3000 express-server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mongoose": "^5.13.2",
    "morgan": "~1.9.1",
    "redis": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
